<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>animation</title>

    <style>
        /*body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f4f4f4;
        }

        .container {
            display: flex;
            gap: 20px;
        }

        .box {
            width: 150px;
            height: 150px;
            background-color: #ff6f61;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            font-weight: bold;
            color: #F4F4F4;
            border-radius: 10px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
            animation: swing 2s ease-in-out infinite alternate;
        }

        .box:nth-child(1) {
            animation-delay: 0s;
        }
        .box:nth-child(2) {
            animation-delay: 0.2s;
        }
        .box:nth-child(3) {
            animation-delay: 0.4s;
        }

        @keyframes swing {
            0% {
                transform: rotate(-5deg);
            }
            100% {
                transform: rotate(5deg);
            }
        }*/
        /*body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: #f8f8f8;
        }

        .logo {
            position: relative;
            width: 120px;
            height: 120px;
        }

        .main-shape {
            position: absolute;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, #FF7F00, #FF4500);
            clip-path: path("M30 90 C20 70, 40 40, 60 30 C80 20, 90 30, 95 50 C100 70, 80 100, 50 110 C20 120, 10 110, 30 90 Z");
        }

        .eye {
            position: absolute;
            width: 15px;
            height: 15px;
            background: #D35400;
            border-radius: 50%;
            top: 35%;
            left: 70%;
        }

        .tail {
            position: absolute;
            width: 30px;
            height: 30px;
            border: 5px solid #FF7F00;
            border-radius: 50%;
            top: 25%;
            left: 10%;
            transform: rotate(-30deg);
        }*/
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: #f8f8f8;
        }

        svg {
            width: 180px;
            height: 180px;
        }
    </style>
</head>
<body>
<!--<div class="container">
    <div class="box"><img src="assets/monkey.jpeg"></div>
    <div class="box"><img src="assets/monkey.jpeg"></div>
    <div class="box"><img src="assets/monkey.jpeg"></div>
</div>-->
<!--<div class="logo">
    <div class="main-shape"></div>
    <div class="eye"></div>
    <div class="tail"></div>
</div>-->
</body>
</html>